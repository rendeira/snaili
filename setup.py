from setuptools import setup

from complemento import nome, versao, url, descricao, autor

setup(
    name=nome,
    version=versao,
    url=url,
    license='MIT',
    author=autor,
    author_email='',
    description=descricao
)
